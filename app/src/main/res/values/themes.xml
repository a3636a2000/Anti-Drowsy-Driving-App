<resources>
    <!-- Base application theme. -->
    <style name="Theme.Donoff" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary</item>
        <item name="colorSecondaryVariant">@color/primary</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:letterSpacing">0</item>

        <!-- Font -->
        <item name="android:fontFamily">@font/notosans_kr_regular</item>
        <item name="fontFamily">@font/notosans_kr_regular</item>

        <item name="buttonStyle">@style/Button_Default</item>
        <item name="imageButtonStyle">@style/ImageButton_Default</item>
        <item name="editTextStyle">@style/EditText_Default</item>

        <!-- Scrollbar -->
        <item name="android:scrollbarSize">5dp</item>
        <item name="android:scrollbarThumbHorizontal">@drawable/scrollbar_thumb</item>
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_thumb</item>
    </style>

    <style name="Theme.Donoff.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/bg_transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.Donoff</item>

        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/primary</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Button_Default" parent="@style/Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:background">@drawable/bg_button_default</item>
        <item name="android:colorButtonNormal">@color/background_secondary</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_button</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Button_Negative" parent="Button_Default">
        <item name="android:backgroundTint">@color/background_secondary</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="ImageButton_Default" parent="@style/Widget.AppCompat.ImageButton">
        <item name="android:paddingStart">25dp</item>
        <item name="android:paddingTop">25dp</item>
        <item name="android:paddingEnd">25dp</item>
        <item name="android:paddingBottom">70dp</item>
        <item name="android:background">@drawable/bg_button_small</item>
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="EditText_Default" parent="@style/Widget.AppCompat.EditText">
        <item name="android:background">@drawable/bg_edittext</item>
        <item name="android:textSize">@dimen/text_editText</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/black</item>
    </style>
</resources>